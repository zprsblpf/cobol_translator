# 任务01：产品入口与输出修正

## 目标

修复当前 no-LLM 单文件输出入口不执行首个 SECTION、明显输出文案污染的问题，并为 deterministic 产品主线做铺垫。

## 泳道

A：产品主线

## 允许修改

- `translator/skeleton_gen/render_skeleton.py`
- `asg/section_visitor.py`
- `test_translation.py`
- `test_smoke.py`
- `docs/操作记录/步骤33-产品入口与输出修正操作记录.md`

## 禁止修改

- `graph/nodes.py`
- `translator/leaf/*`
- `translator/skel/*`
- `translator/rules.py`

## 依赖

- 规划：`docs/详细设计/本地确定性翻译完善规划.md`
- 产物样例：`tests/fixtures/minimal.cob`

## 实施步骤

1. 增加测试：`render_skeleton(minimal)` 的 `execute()` 应实际调用首个 SECTION。
2. 修 `render_skeleton._entry()`，把注释改为真实调用。
3. 增加测试覆盖 `asg/section_visitor.py` 写 IO 空 catch 文案为 `// (空)`。
4. 修复 `// (绌?`。
5. 更新操作记录。

## 验收命令

```bash
python -m unittest test_smoke -v
python -m unittest test_translation.TestMainlineSectionViaAsg -v
python scripts/check.py
```

## 完成标准

- minimal no-LLM 产物 `execute()` 实际调用首个 SECTION。
- 不再生成 `// (绌?`。
- 回归测试通过。

## 合并备注

可与任务02、任务03、任务04 并行。潜在冲突文件为 `test_translation.py`。

