# 本地确定性翻译后续开发计划

日期：2026-07-02

## 当前状态

已完成并通过本地闸口：

- T01 产品入口与输出修正。
- T02 结构化 unsupported 基座。
- T03 覆盖扫描器雏形。
- T04 快速验证入口。
- T05 `config/rules` 规则资产目录补强。
- T06 UNSTRING 叶子固化。
- T07 INSPECT 叶子固化。
- T08 SEARCH 叶子固化。
- T09 复杂 STRING 补强。

当前全量验收：`python scripts/check.py` 通过，242 tests OK，skipped=2。

## 未完成任务

### P1 收尾

#### T15 ASG fallback 可观测

目标：`translator/skeleton_gen/body_context.py` 中 ASG -> legacy fallback 不再静默发生，至少进入结构化 unsupported/可审计事件或报告入口。

建议允许修改：

- `translator/skeleton_gen/body_context.py`
- `translator/unsupported.py`
- `test_translation.py`
- `docs/操作记录/步骤42-ASG-fallback可观测操作记录.md`

验收：

```bash
python -m unittest test_translation.TestMainlineSectionViaAsg -v
python scripts/check.py --suite asg
python scripts/check.py
```

#### T16 deterministic 多文件报告输出

目标：`python main.py input.cob --deterministic --output out` 输出 Java、`unsupported_report.json`、`coverage_report.md`、`review_checklist.md`，且 deterministic 路径不调用 LLM。

建议允许修改：

- `main.py`
- `translator/assemble.py`
- `scripts/scan_rule_coverage.py`
- `translator/unsupported.py`
- `test_smoke.py`
- `docs/运行验收.md`
- `docs/操作记录/步骤43-deterministic多文件报告输出操作记录.md`

验收：

```bash
python main.py tests/fixtures/minimal.cob --deterministic --output /tmp/cobol-det-minimal
python scripts/check.py --suite deterministic
python scripts/check.py
```

#### T17 文档状态同步

目标：同步项目总览、运行验收、翻译标准和任务矩阵状态，明确 01-09/T05 已完成，T10-T16 待开发。

建议允许修改：

- `docs/架构索引/项目总览.md`
- `docs/运行验收.md`
- `docs/并行开发/任务矩阵.md`
- `docs/操作记录/步骤44-文档状态同步操作记录.md`

验收：

```bash
python scripts/check.py --suite quick
python scripts/check.py
```

### P2 控制流与条件

#### T10 EVALUATE TRUE 支持

目标：支持保守子集 `EVALUATE TRUE WHEN <condition>`，复用 `translate_condition` 生成 if/else-if 链。

风险：`EVALUATE` 当前在 ASG/control visitor 中已有 switch 壳，需避免破坏普通 `EVALUATE subject`。

验收：

```bash
python -m unittest test_translation.TestAsgControlVisitor test_translation.TestLeafControlExtract -v
python scripts/check.py --suite leaf
python scripts/check.py
```

#### T11 88 条件名支持

目标：建立 88 条件名解析/映射的最小链路，让 `translate_condition` 能解析已知 condition-name。

风险：需要从 WS 变量树携带条件名元数据，优先做最小 fixture。

验收：

```bash
python -m unittest test_translation.TestLeafCondExtract -v
python scripts/check.py --suite leaf
python scripts/check.py
```

#### T12 GO TO DEPENDING ON

目标：支持 `GO TO A B C DEPENDING ON idx` 的保守 Java switch/if 分派，未知目标结构化 unsupported。

风险：涉及控制流跳转，不建议与 T10/T11 同时改同一 visitor 分支。

验收：

```bash
python -m unittest test_translation.TestAsgControlVisitor test_translation.TestSkelFlowDispatch -v
python scripts/check.py --suite asg
python scripts/check.py
```

### P2/P3 数据语义

#### T13 OF/IN 字段限定消歧

目标：表达式和赋值解析支持 `FIELD OF GROUP` / `FIELD IN GROUP` 消歧，减少字段碰撞和误绑定。

风险：影响 `_operand`、`_lvalue`、字段命名上下文，应先用小 fixture 锁定。

验收：

```bash
python -m unittest test_translation.TestLeafMoveExtract test_translation.TestLeafCondExtract -v
python scripts/check.py --suite leaf
python scripts/check.py
```

#### T14 REDEFINES/数组视图增强

目标：增强 REDEFINES 与数组视图翻译，减少数据视图 unsupported。

风险：影响 WSAA 渲染与存储视图，建议最后单独做。

验收：

```bash
python -m unittest test_translation.TestSignedOverpunch test_translation.TestVariableResolver -v
python scripts/check.py
```

## 推荐执行顺序

1. T15：先让 fallback 可观测，降低后续改动盲区。
2. T17：同步文档状态，避免任务矩阵继续把已完成项视为待办。
3. T16：形成 deterministic 产品闭环。
4. T10、T11：条件/控制流基础。
5. T12：复杂跳转，单独开发。
6. T13：字段限定消歧。
7. T14：REDEFINES/数组视图增强。

## 合并原则

- 每个任务继续遵守 `docs/并行开发/Agent执行协议.md`。
- 所有新增 unsupported 必须有稳定 rule_id。
- 所有新增规则必须登记到 `config/rules/*.yaml`。
- 每个任务完成后运行任务验收和 `python scripts/check.py`。
