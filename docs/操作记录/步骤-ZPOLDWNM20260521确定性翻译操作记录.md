# 操作记录 · 确定性翻译 ZPOLDWNM20260521.cob → Java

> 执行日期：2026-07-06
> 对应设计文档：`docs/详细设计/步骤05-cob到Java翻译引擎详细设计.md`
> 执行人：Reasonix Code
> 模式：确定性翻译（`--no-llm`，零外部依赖）
> 结果：✅ 翻译完成

---

## 1. 目标

将 `ZPOLDWNM20260521.cob`（PROGRAM-ID `ZPOLDWNM`，16858 物理行）使用本项目确定性翻译引擎翻译为 Java，
产出骨架 + 规则翻译的叶子语句，不依赖 LLM。

## 2. 源文件信息

| 属性 | 值 |
|------|-----|
| 路径 | `E:\xincheng\项目\85\源码\源码\CBL FILES\ZPOLDWNM20260521.cob` |
| 大小 | 1,372,114 字节 |
| 行数 | 16,858 |
| 版本 | 2026-05-26（带日期后缀的更新版） |

## 3. 执行步骤

### 3.1 解析验证

```bash
python main.py "E:\xincheng\项目\85\源码\源码\CBL FILES\ZPOLDWNM20260521.cob" --parse-only
```

解析结果：

| 指标 | 值 |
|------|-----|
| PROGRAM-ID | ZPOLDWNM |
| SECTION 数量 | 133 |
| 变量数 | 888 |
| 连接节参数 | 2（LETCMNT-PARAMS, PMSPNT-PARAMS） |
| COPY 引用 | 136 |
| 高风险含 GOTO 的 SECTION | 83（占 62.4%） |
| REDEFINES | 21 |

✅ 解析通过，文件结构清晰可识别。

### 3.2 确定性翻译

```bash
python main.py "E:\xincheng\项目\85\源码\源码\CBL FILES\ZPOLDWNM20260521.cob" --no-llm --output output/zpoldwnm_20260521
```

❖ 耗时：秒级完成
❖ 模式：deterministic rules only

### 3.3 输出验证

产出文件：`output/zpoldwnm_20260521/Zpoldwnm.java`

| 指标 | 值 |
|------|-----|
| Java 行数 | 7,729 |
| 方法数 | 136 |
| 文件大小 | 436,394 字节 |
| 总 TODO | 137 |

TODO 分类明细：

| 类别 | 数量 | 说明 |
|------|:----:|------|
| TODO-LEAF | 69 | 复杂叶子（STRING/COMPUTE等规则未覆盖） |
| TODO-IF | 48 | 复杂条件（多个AND/OR组合） |
| TODO-GOTO | 7 | GO TO 控制流标注 |
| TODO-CALL | 4 | 非常规CALL调用 |
| TODO PERFORM (THRU) | 2 | THRU区间端点不可确定 |
| 其他 | 7 | 其余标注项 |

TODO 率：137 / 7729 ≈ **1.8%**，主体代码确定性规则成功覆盖。

## 4. 输出文件结构

单文件 `Zpoldwnm.java`，含：

- 包声明 `package zpoldwnm;`
- 引用实体类列表（72 个 COPY 记录映射）
- **136 个方法**（133 个 SECTION + 辅助方法）
- GO TO 控制流由 `switch(__pc)/FLOW/break` 状态机构造
- PERFORM…THRU 合成区间方法
- 未处理的 TODO 以 `// TODO-XXX:` 注释保留，便于后续逐项人工核对或 LLM 补齐

## 5. 后续建议

- **TODO 清理**：137 个 TODO 需要逐项核对。建议按类别优先处理：
  1. TODO-GOTO（7 个）— 控制流完整性最敏感
  2. TODO-CALL（4 个）— 调用正确性
  3. TODO PERFORM THRU（2 个）— 区间完整性
  4. TODO-IF（48 个）— 条件逻辑
  5. TODO-LEAF（69 个）— 叶子语句
- **LLM 兜底**：如果有本地 LLM 端点（如 vLLM），可对 TODO 区域做增量填充
- **编译验证**：需要补齐 COPY 记录的 Java 实体类后才能编译

## 6. Token 使用分析

| 消耗项 | 量级 |
|--------|------|
| 解析阶段 | 少量（纯 Python） |
| 翻译阶段 | 中等（16858 行循环处理，无 LLM 调用） |
| 输出验证 | 少量 |
| **总 LLM Token** | **0**（纯确定性路径） |
