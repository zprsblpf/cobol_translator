# 步骤35 覆盖扫描器雏形操作记录

日期：2026-07-01

## 本次改动

- `scripts/scan_rule_coverage.py`
  - 新增 COBOL 动词覆盖扫描器。
  - 使用 `parser.cobol_parser.parse` 解析程序，复用 `translator.segmenter` 统计语句首动词。
  - 对 `simple` 叶子语句调用 `translate_leaf_stmt` 判定 supported/unsupported。
  - 对 `IF`、`EVALUATE`、`PERFORM` 结构语句按 segmenter 已识别结构计入 supported。
  - CLI 支持 `text`、`json`、`markdown` 输出；默认打印终端摘要并输出 JSON。
- `test_smoke.py`
  - 新增 `TestRuleCoverageScannerSmoke`，覆盖 `tests/fixtures/minimal.cob` 的 MOVE supported 计数。

## 验收

- `python scripts/scan_rule_coverage.py tests/fixtures/minimal.cob`
  - exit 0；输出 `supported=4 unsupported=0 total=4`，动词包含 `MOVE supported=2`、`PERFORM supported=1`、`GOBACK supported=1`。
- `python -m unittest test_smoke -v`
  - 2 tests OK。
- `python scripts/check.py`
  - exit 0；内部 unittest 221 tests OK，skipped=2，并完成 minimal.cob parse-only、skeleton、no-LLM 翻译检查。

## unsupported rule_id

- 本次未新增 unsupported rule_id。

## 遗留风险

- 本任务未接入结构化 unsupported 事件，本步先按动词级 supported/unsupported 统计。
