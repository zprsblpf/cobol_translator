# 步骤17　旁路建相2「自研轻量 ASG」操作记录

对应设计：`../详细设计/步骤17-旁路建相2-ASG设计.md`（🟢已实现）
上位路线：`../详细设计/架构演进-三相分层(预处理-ASG-visitor)初步设计.md` §5-项2
执行日期：2026-06-25 设计认可 → 2026-06-26 落地 + 回填
分支：`refactor/graph-orchestration-only`

---

## 1. 做了什么（绞杀分期② 第一刀：旁路建相2 ASG）

在相1（预处理，步骤15/16）之上**旁路并存**地建起相2「带类型 ASG + 过程注册表 + 引用解析」，
先只供**单点 visitor**（GO TO → Java）消费自证，**零改旧 parser/segmenter/rules**，旧管线继续当回归闸。

新建 `asg/` 包（5 文件，均瘦而专、逻辑行 100 内）：

| 文件 | 职责 |
|---|---|
| `asg/nodes.py` | `@dataclass` 平铺节点：`Program/Section/Paragraph` + `IfStmt/EvaluateStmt/PerformStmt/GotoStmt/CallStmt/Leaf` |
| `asg/registry.py` | `ProcUnit/ProcRef/ProcRegistry`(`resolve/resolve_thru` 闭区间)+`SymbolTable`(`lookup`)；复用 `body_context._build_proc_order` |
| `asg/builder.py` | `build()` + 唯一嗅探点 `_lift()`：复用 `segmenter.segment/split_paragraphs` 取裸 Stmt → 提升为带类型节点 |
| `asg/visitor.py` | `AsgVisitor`(`visit_<类名>` 分派)+单点 demo `GotoJavaVisitor`（复刻 `_sk_control`，不 import rules） |
| `asg/__init__.py` | 瘦门面 + 唯一入口 `build_asg(program)` |

---

## 2. 命令与产物

```bash
# 落地后自检（无副作用）
python3 -m unittest test_translation -v          # 全量单测（旧回归闸 + 新增 ASG 单测）
python3 scripts/regress_config_snapshot.py        # config 快照（旧路径零 diff 硬闸）
```

产物：
- `asg/`（5 文件，新包）
- `test_translation.py` 追加 `TestAsgBuild`（3 例）+ `TestAsgGotoVisitor`（2 例）
- `docs/详细设计/步骤17-…设计.md` 回填 §9 实现结果，状态 🔶→🟢
- `docs/架构索引/项目总览.md` 补 asg/ 包（文件清单 5 行 + 调用关系「三相演进旁路」+ 包简述 + 详细设计索引 4 行）

---

## 3. 校验结果（设计 §5 四项 + 旧回归，逐项）

| 项 | 验收点 | 结果 |
|---|---|---|
| 旧回归闸 | `test_translation` 旧 48 用例 | ✅ 全绿（旧代码未动） |
| 旧回归闸 | `regress_config_snapshot.py` | ✅ exit 0，快照不变 |
| ① | `build_asg` 跑通 + SECTION 数一致 + 节点树非空 | ✅ `test_build_section_count_and_nonempty` |
| ② | GO TO 目标解析到真实过程单元 | ✅ `test_goto_target_resolved`（`target.unit` 非 None） |
| ③ | PERFORM A THRU C → `resolve_thru` 取 [A..C] 闭区间 | ✅ `test_perform_thru_closed_interval` |
| ④ | `GotoJavaVisitor.visit` 逐字符等于 `rules._sk_control`（flow_label=None） | ✅ `TestAsgGotoVisitor`（EXIT/未知段两路） |

全量：`Ran 53 tests ... OK (skipped=2)`（旧 48 + 新 5；2 例环境跳过）。

开发期冒烟（非提交单测，cob 不在仓内）：`scripts/spike_proleap/cleaned_ZPOLDWNM.cob`
→ 131 SECTION / 342 paragraph / 206 GotoStmt / 194 解析到单元，`build_asg` 无异常。

---

## 4. 提交

| commit | 内容 |
|---|---|
| `dc14152` | 步骤15/16 相1 预处理落地（preprocess 包 + 列裁剪/方言归一吸收） |
| `3c45e1d` | 步骤17 相2 自研轻量 ASG 旁路落地（绞杀分期② 单点 GO TO 验证） |

均已 push 到 `origin/refactor/graph-orchestration-only`。单测 + 文档回填本次随后补提。

---

## Token 使用分析

- **主要消耗**：① 续接会话的上下文重放（本任务跨 compact，前序 asg/ 代码 + 设计文档全文在每轮复利计费）；
  ② 设计文档（200 行）+ 架构索引（246→260 行）两份长文档的精读与多处编辑；
  ③ 两个探针脚本（probe_asg/probe_eq）跑实测取真实计数，避免臆测断言数字。
- **量级**：中等。代码产出小（asg 5 文件早已在前序会话落地，本次仅补单测 5 例）；
  主要花在文档回填（设计 §9 + 架构索引 4 处）与逐项校验回显。
- **节流措施**：用 `grep -n` 精定位编辑锚点、探针脚本只打印计数不回显大产物、
  单测 tail 截断输出；未无脑全文读 rules.py（仅精读 `_sk_control` 25 行）。
- **Context 提示**：本会话已跨一次 compact 且叠加了相1/相2 两步的代码+文档，context 偏中高；
  后续若进入「项3 逐类迁 visitor」建议 `/clear` 或新开会话再叠加，避免历史复利。
=======
对应设计：`../详细设计/步骤17-旁路建相2-ASG设计.md`（🟢已实现）。
关联演进：`../详细设计/架构演进-三相分层(预处理-ASG-visitor)初步设计.md` §5-项2。

> 本步分两段落地：① 代码（commit `3c45e1d`，2026-06-26 早）；② 收尾——单测 + 状态回填 +
> 本操作记录 + 项目总览（2026-06-26，本记录覆盖②并复盘①）。

---

## 1. 讨论与决策脉络

- 接续 `/clear` 后的「继续本项目」：先读规划/设计/git，定位到步骤17 代码已合并（PR #8），但
  commit `3c45e1d` 自述「状态回填/单测/操作记录/架构索引按用户指示后续再补」——四项收尾全欠。
- 与用户确认继续方向：选「补步骤17 收尾（推荐）」——先把已认可设计 §5 的四项验收落成单测、跑回归，
  再回填文档。理由：code 无测试是当前最大缺口，且 §5 自证闸是设计明文要求，属「完成当前步」而非抢跑步骤18。
- **关键现实约束**：仓库内无 `ZPOLDWNM.cob` 夹具（只在 docs 有引用），设计 §5 的 ①② 原文依赖它。
  决策：①②改用**内联临时 .cob**（仿既有 `TestProcStartCommentImmune` 的 `mkstemp`+`parse`+`unlink`），
  ③用合成 `ProcUnit` 直测 `ProcRegistry`，④构造 `GotoStmt` 与 `rules._sk_control` 逐字符对比。
  验收语义不变、不引入对外部源的依赖。

## 2. 执行步骤与校验

| # | 动作 | 命令 / 产物 | 结果 |
|---|---|---|---|
| 1 | 读 asg/ 五文件确认实现契约 | Read `asg/{__init__,nodes,registry,builder,visitor}.py` | `build_asg(program)` 单入口；`_lift` 唯一嗅探点；visitor `…EXIT→return` / 未知→`TODO-GOTO+return` |
| 2 | 确认 ④ 对比基准 | Grep `rules._sk_control`（L1337）+ `_ind`（`_ind(0)==""`）+ `Ctx` 默认（`flow_label=None`） | EXIT/未知两支与 visitor 末两支同构，indent=0 时可逐字符对齐 |
| 3 | 写 4 个测试类（11 用例） | Edit `test_translation.py`：`TestAsgBuild`/`TestAsgRegistryThru`/`TestAsgGotoVisitor` | 见 §3 |
| 4 | 跑新增单测 | `python -m unittest test_translation.TestAsgBuild test_translation.TestAsgRegistryThru test_translation.TestAsgGotoVisitor -v` | **Ran 11 tests … OK** |
| 5 | 全量回归（旧路径零影响硬闸） | `python -m unittest test_translation -v` | **Ran 75 … OK (skipped=2)**（旧 64 + 新 11，2 skip=离线 LLM） |
| 6 | config 快照闸 | `python scripts/regress_config_snapshot.py` | 正常出 JSON，exit 0（ASG 零触碰 config，天然零 diff） |
| 7 | 文档收尾 | 设计 §3 状态 🔶→🟢 + §9 实现结果；本操作记录；项目总览加 asg/ 条目 | 完成 |

## 3. 验收四项 → 单测落实

- **① 结构提升**（`TestAsgBuild.test_build_runs_and_sections_match_parse` / `test_registry_attached_to_root`）：
  内联两 SECTION + EXIT paragraph 程序 `parse`→`build_asg`，`program_id`/SECTION 数与 parse 一致、
  节点树非空、`Program.registry` 是 `ProcRegistry` 且含 `1000-MAIN`/`1000-EXIT` 单元。
- **② 引用解析**（`test_goto_target_resolved_to_unit`）：`GO TO 1000-EXIT` 提升出 `GotoStmt`，
  `target.name=="1000-EXIT"` 且 `target.unit` 解析到真实 paragraph 单元（非 None）。
- **③ THRU 区间**（`TestAsgRegistryThru` 5 用例）：`resolve_thru("A","B")` 取 `[A,M,B]` 闭区间
  （中间不丢、Z 不越界）；B 缺失/`NOPE`→`[A]`；B 在 A 前→`[A]`；A 未知→`[]`；按名大小写不敏感。
- **④ 单点自证**（`TestAsgGotoVisitor` 3 用例）：`GotoJavaVisitor.visit(GotoStmt(…))` 对 `…EXIT`
  与未知段的输出 == `rules._sk_control`（`flow_label=None`/`indent=0`/目标不在 `known_sections`）逐字符；
  目标缺失→`["return;"]`。

## 4. 与设计的偏差（如实记录）

- `builder.build(program)` 单参（registry 在内部 `_build_reg` 构建并挂 `Program.registry`），
  未取 §2.3 草图的 `build(program, registry)` 双参。编排更内聚，对外仍 `build_asg(program)` 单入口，契约不变。
- §5 ①②原文以 `ZPOLDWNM.cob`（131 SECTION / 127 go_tos）为基准，因无夹具改内联程序验同等语义（见 §1 约束）。

## 5. 落盘产物

- 代码：`asg/`（commit `3c45e1d`，本步未改一行 asg 代码，仅补测试与文档）。
- 单测：`test_translation.py` 新增 3 类 11 用例。
- 文档：本记录 + 设计 §9 实现结果 + `docs/架构索引/项目总览.md`（asg/ 条目）。

## 6. Token 使用分析

- **主要消耗**：
  - 一次性读 `test_translation.py` 全文（~910 行，为对齐既有测试风格/夹具手法，必要）——单轮最大块。
  - 读 `项目总览.md` 全文（~255 行，收尾需精确插入 asg/ 条目）。
  - asg/ 五文件 + rules.py 片段精读（定位 `_sk_control`/`_ind`/`Ctx`，grep 定位后只读片段，省）。
- **工具轮数**：探查（glob/grep/read）约 10 余轮，编辑+跑测试约 6 轮；无反复调试（单测一次全绿）。
- **量级**：中等。无大文件全量重复读入；回归测试 0.27s 跑完，无迭代重跑开销。
- **节流点**：`_sk_control`/`Ctx` 用 grep 精确定位行号后只读片段，未全读 rules.py（~1400 行）。