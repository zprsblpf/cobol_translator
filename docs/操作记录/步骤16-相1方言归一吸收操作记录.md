# 步骤16　相1「方言归一」吸收　操作记录（绞杀分期①续）

日期：2026-06-25　对应设计：`../详细设计/步骤16-相1方言归一吸收设计.md`（🟢已实现）

## 1. 做了什么

相1 内**第一处有意行为变更**：吸收方言归一（本步＝`GO 段名` → 标准 `GO TO`），补回主线漏检的 GO TO。
全程按**正本链自上而下**：`docs/翻译标准/`（用户源头）→ `config/specs/`（派生模板）→ 代码（只读应用）。

## 2. 产物清单（自上而下）

| 层 | 文件 | 动作 | 说明 |
|---|---|---|---|
| 源头 | `docs/翻译标准/流程结构.md` | 新建·转正 | 「流程结构（控制流）」分类标准；§一 GO TO 方言归一 + 边界（IF/EVALUATE/PERFORM 留占位） |
| 模板 | `config/specs/segmentation_spec.yaml` | 改 | 新增 `dialect_normalization.go_to`（pattern/replacement/quote_guard） |
| 访问 | `config/grammar_loader.py` | 改 | 新增 `dialect_rules()`（读 config + 预编译，`lru_cache`） |
| 实现 | `preprocess/dialect.py` | 新建 | `normalize` 读 `dialect_rules()` 应用 + `_segment_by_quotes`/`_apply_outside_quotes` 引号保护；**无方言正则字面量** |
| 接线 | `preprocess/line_stream.py` | 改 | `CleanSource.strip_line` 列清洗后接 `dialect.normalize` |
| 测试 | `test_translation.py` | 改 | 新增 `TestDialectNormalize`（4 例） |
| 回填 | 设计/架构索引 | 改 | 步骤16 🟢 + §8 实现结果；总览加 `preprocess/dialect.py` + config 节 |

## 3. 校验（行为变更 → 比对式验收）

- **`dialect.normalize` 直测**：省 TO 补全 / `GO TO` 幂等 / `GOBACK` 不动 / 引号内 `'GO HOME'` 不动，**全 OK**。
- **go_tos 前后比对**（改前基线见 scratchpad/goto_before.json）：
  - `ZPOLDWNM.cob`：123 → 127，**新增 4** = `2060-RECEIPT`/`5390-EXIT`/`6390-EXIT`/`7390-EXIT`，**零减少**；
  - `SBSURCALC.cbl`：7 → 8，新增 `C290-EXIT`，**零减少**。
  - 逐处核对：新增项**全为探查 §0 的真 `GO 段名`**（其余如 3190-EXIT 等本就被标准 GO TO 检出），**无误伤**。
- **单测**：`test_translation` **48 OK**（原 44 + 新 4，2 LLM skip）。
- **回归快照**：`regress_config_snapshot` 仍**零 diff**（不解析 .cob，符合预期）。

## 4. 关键纠偏（本步最重要的过程修正）

初版设计把 `GO→GO TO` 正则**写进 `dialect.py` 代码** ＝ 往代码塞方言魔数，违背「config 正本驱动」。
用户指出后修正为**正本链三层级联**：规则的「是什么」(pattern/replacement) 全在 config，代码只管
「怎么安全套用」(引号保护、遍历规则)。沉淀为记忆 `module-roles-config-knowledge-design`（新标准必先进 docs/翻译标准）。

## 5. 下一步

- 既有 `columns._CHANGE_TAG_RE`（步骤06 硬编码的变更标记正则）亦属格式归一，宜后续一并迁入 config `dialect_normalization`。
- 相2（自研 ASG）——见 `架构演进-三相分层…初步设计.md` §5.2。

## Token 使用分析

- **主要消耗**：① 现状/语料探查——精读 `segmentation_spec.yaml`/`grammar_loader.py`/`变量定义.md`，
  grep 语料 GO 方言分布并逐条核 13 处真伪（必要，定落点/正则）；② 工具调用约 20 轮（探查→设计→
  正本链修正→自上而下落地→比对→回填），含一次设计纠偏（config 魔数）往返；③ 代码产物小
  （`dialect.py` 1 新文件 < 100 逻辑行 + 5 处小改）。
- **量级**：中等。设计纠偏 + 翻译标准草稿 + 多文档回填占文本输出大头；代码调试 0 迭代（一次比对即全绿）。
- **Context 状态**：本会话已连做步骤15+16 两步、含多轮设计往返，**context 已偏多**。建议本步收尾后
  `/clear` 或新开会话再继续相2，避免后续每轮复利重算前文。
