# 步骤15　相1「预处理」独立成相　操作记录（绞杀分期①）

日期：2026-06-25　对应设计：`../详细设计/步骤15-相1预处理独立成相设计.md`（🟢已实现）

## 1. 做了什么

落地三相架构绞杀式迁移**第一刀**：把散落的预处理逻辑收拢为边界清晰的相1 包 `preprocess/`，
旧 parser 经薄重导出/薄委托消费其产物，**零行为变化**。

## 2. 产物清单

| 文件 | 动作 | 说明 |
|---|---|---|
| `preprocess/columns.py` | 新建 | `parser/cobol_columns.py` 整体迁入（列处理正本，逻辑逐字不改） |
| `preprocess/line_stream.py` | 新建 | `CleanLine`/`CleanSource` + `build`/`strip_line`/`as_pairs`（搬自旧 `_strip_cobol_line`/`_clean_lines`） |
| `preprocess/__init__.py` | 新建 | 瘦门面：导出原语+CleanSource，唯一入口 `preprocess_file(path)` |
| `parser/cobol_columns.py` | 改 | 改薄重导出 `preprocess.columns`（旧 import 路径不破） |
| `parser/cobol_parser.py` | 改 | `_strip_cobol_line`/`_clean_lines` 改薄委托相1；`parse()` 逻辑未动 |
| `docs/详细设计/步骤15-…设计.md` | 回填 | 🔶→🟢，补 §9 实现结果 |
| `docs/架构索引/项目总览.md` | 同步 | 新增 `preprocess/` 包条目与简述 |

## 3. 校验（回归闸，§6 全过）

```
PY=/data/models/llm-fa312/bin/python
# 改前基线
$PY scripts/regress_config_snapshot.py > snap_before.json   # 10546B
$PY -m unittest test_translation                            # 44 OK (skipped=2)
# 改后
$PY scripts/regress_config_snapshot.py > snap_after.json
diff snap_before.json snap_after.json                       # 零 diff ✅
$PY -m unittest test_translation                            # 44 OK (skipped=2) ✅
```

- 回归快照：**逐字节零 diff**（10546B）。
- 单测：**44 OK**（2 LLM skip），与改前一致。
- 导入面不破：`grammar_loader`/`decompose.lines`/`cobol_columns`/`cobol_parser`/`preprocess` 均导入成功。
- 产物一致：`/home/zp/Documents/ZPOLDWNM.cob` 上 `cobol_parser._clean_lines()` 与
  `preprocess.preprocess_file().as_pairs()` **9636 行逐项相等**；`parse()` 跑通
  （PROGRAM-ID=ZPOLDWNM，133 SECTION，888 WS，136 COPY 引用）。

## 4. 下一步

- **步骤16「相1 方言归一吸收」**：CRLF 归一 + `GO→GO TO` 归一（行为变更，走前后产物比对）。
- 真·COPY 展开：单独步骤。

## Token 使用分析

- **主要消耗**：① 现状摸排——精读 `cobol_parser.py`(257行)/`cobol_columns.py`(60行)/`pipeline.py`/`segmenter.py`(206行)
  各一遍定位预处理分布（最大块，但必要且一次性）；② 工具调用约 15 轮（探查→设计→落地→校验→回填），
  以 grep/精读定位、批量校验为主，未无脑全文重读；③ 代码产物小（3 新文件均 < 100 逻辑行，2 处薄改）。
- **量级**：中等偏低。设计文档 + 回填占文本输出大头；调试迭代为 0（一次校验即全绿）。
- **Context 状态**：本会话从 `/clear` 起，单任务闭环，context 处于健康区间，无需提醒清理。
