# 步骤31 叶子翻译统一入口操作记录

日期：2026-06-30

## 本次改动

- `translator.leaf`
  - 新增 `translate_leaf_stmt(tokens, ctx)`，作为 rules 与 ASG visitor 共享的叶子分派入口。
  - 导出 `translate_leaf_stmt`。
- `translator.rules`
  - `rules._dispatch_leaf` 保留名称，但实现降为兼容 wrapper，委托 `translate_leaf_stmt`。
  - `translate_leaf` 保留原有 `struct_rebind` 包裹逻辑。
- `asg.visitor`
  - `LeafJavaVisitor.visit_Leaf` 改为调用 `translate_leaf_stmt`，未命中时仍输出 `// TODO-LEAF: ...`。
- `test_translation.py`
  - 新增 `TestUnifiedLeafEntry`，锁定统一入口、旧 wrapper、rules 与 ASG visitor 的输出一致性。
- `docs/superpowers`
  - 新增步骤31设计 spec 与执行 plan。

## 验收

- `python -m unittest test_translation.TestUnifiedLeafEntry -v`
  - 4 tests OK。
- `python -m unittest test_translation.TestUnifiedLeafEntry test_translation.TestAsgLeafArithVisitor test_translation.TestAsgControlVisitor test_translation.TestMainlineSectionViaAsg test_translation.TestMainlinePendingRangeViaAsg -v`
  - 17 tests OK。
- `python -m unittest test_translation`
  - 186 tests OK（skipped=2）。
- `python scripts/check.py`
  - exit 0；内部 unittest 187 tests OK（skipped=2），并完成 minimal.cob parse-only、skeleton、no-LLM 翻译检查。
