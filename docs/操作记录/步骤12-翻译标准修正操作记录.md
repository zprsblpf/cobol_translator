# 步骤12 · 翻译标准修正（符号 / PERFORM THRU / WS 持久性 / 命名碰撞）· 操作记录

对应设计：`../详细设计/步骤12-翻译标准修正(符号-THRU-持久性-命名碰撞)设计.md`（🟢已实现）
执行日期：2026-06-09
触发：用户「认可+继续」——采纳设计全部推荐项（S-1=overpunch-on-negative、W-1=只标注不自动探测、N-1①父前缀消歧）后，4 条修正一并落地。

---

## 1. 已确认的决策（落地前固化）

| 决策 | 采纳 |
|---|---|
| S-1 负号在存储串怎么表示 | 设计推荐③（旁路 sign 标志）经读 `storage.py` 确认**不可实现**（定宽串靠 substring 切片消费，无处安放旁路标志）→ 实改①**overpunch-on-negative**：正/无符号保纯数字串（零回归），仅负值末位叠加 overpunch（`0-9 ⇄ }JKLMNOPQR`） |
| S-2 豁免白名单 | 初始**空**（全部保符号），按实测反例再加 `abs_whitelist` |
| ② THRU 展开范围 | 仅两端均为**已知 SECTION** 且 B 在 A 之后才展开；paragraph 级端点 → 退化 TODO，不臆测中间段 |
| W-1 WS 跨调用持久性 | 选**不自动探测**：仅在生成容器类头注释就地标注假设，留人工判断 |
| N-1① 命名碰撞 | **父组名前缀**改名保留（`parentJn + Pascal(jn)`）+ TODO，父前缀仍冲突 → `jn_2/jn_3` 兜底；绝不静默丢字段 |

## 2. 改动文件

| 文件 | 改动 |
|---|---|
| `config/specs/wsaa_translation_spec.yaml` | 假设 `signed_display_abs`→`signed_display_overpunch`（保符号正本）；`working_storage` 加 `cross_call_persistence` 假设 |
| `config/specs/skeleton_spec.yaml` | `block_grammar.perform` 新增 `thru` 模板（single_unit/cross_section/unresolved）+ 改正旧错误样例（原样例只译 A、丢中间段） |
| `config/mappings/naming_conventions.yaml` | 新增 `collision_policy`（parent_prefix + ordinal_suffix 兜底 + keep_first + emit_todo） |
| `translator/wsaa/storage.py` | `_toDigits` 负值末位 overpunch 编码；新增 `_deOverpunch`；`_fromDigits` 经解码还原；`num_from_digits` int/long 经 `_deOverpunch`；docstring/注释对齐 |
| `translator/wsaa/render_view.py` | REDEFINES 视图注释由「取绝对值」改为「末位 overpunch 保号」 |
| `translator/rules.py` | `Ctx` 加有序 `section_order`；新增 `_perform_range()`（跟随 skeleton_spec.thru 展开 THRU 区间）；`_sk_perform` 裸过程名分支改调它 |
| `translator/skeleton_gen/body_context.py` | `build_body_ctx` 填 `section_order`（全程序 SECTION 名顺序） |
| `translator/wsaa/render_class.py` | 新增 `_disambiguate()`；叶子撞名分支改名保留+TODO（取代「重复名跳过」静默丢）；透传 `parent_jn`；容器类头注释加步骤12 §3 持久性假设 |
| `translator/wsaa/render_field.py` | `render_field` 加 `name_override` 形参（消歧改名用，类型/初值不变） |
| `test_translation.py` | 新增 `TestPerformThru`×4、`TestSignedOverpunch`×3、`TestNameCollision`×2，共 9 用例 |

## 3. 校验结果

```
python -m unittest test_translation
Ran 38 tests in 0.018s
OK (skipped=2)        # 2 skip = 本地 vLLM 离线，与本步骤无关
```

逐项：
- ✅ ① 负值 `_toDigits → _deOverpunch` 原样还原（含符号），正/无符号纯数字串零回归；生成 helper 含 `_OVP`/`_deOverpunch`/`v.signum() < 0`，不再无条件 abs。
- ✅ ② `A THRU B` 跨段展开区间内每段（中间段不丢）；A==B/单段退化单调用；端点非已知 SECTION → TODO + pA()；无 THRU 历史行为不变。
- ✅ ④ 撞名叶子改名保留（`wsaaGroupBWsaaDup`）+ 标「命名碰撞」TODO，基名只一份，旧「重复名跳过」已移除。
- ✅ ③ 容器类头注释含跨 CALL 持久性假设。
- ✅ 真程序 `ZPOLDWNM` 端到端（`scripts/translate_wsaa.py`）无异常：overpunch helper 正确产出，签名金额走保符号路径，本程序无命名碰撞（无误报）。

## 4. 已知限制 / 范围外
- ② 仅展开已知 SECTION 端点；paragraph 级 THRU 区间解析未做（退化 TODO，P-1③）。
- ④ 仅消歧**声明**；撞名二者实为不同字段时，**引用处** OF/IN 限定解析未做（标 TODO 留人工核对）。
- ① 不建模正号 overpunch（`{ABC…`）；本翻译自产自消，正值纯数字串自洽。
- 评审归为「尚未覆盖的构造」（88 setter/区间值、OCCURS DEPENDING ON、GO TO DEPENDING ON、MOVE 类型规则）属新增覆盖，另开步骤。

---

## Token 使用分析
- **主要消耗**：① 一次性 `git diff` 全量回显（~250 行）用于核对改动；② 精读设计文档 §6 回填节（~30 行）+ 头部 60 行；③ 读步骤11 操作记录（~70 行）对齐体例。
- **量级**：低。无大文件全文读，工具轮数约 6–8 轮，单测产物 tail 截断回显。
- **Context 状态**：本会话从 `/clear` 起，仅排查 + 收尾，context 处于低位，可继续叠加后续步骤。
