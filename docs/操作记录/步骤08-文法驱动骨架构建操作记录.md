# 步骤08 — 文法驱动骨架构建 操作记录（2026-06-04）

对应设计：docs/详细设计/步骤08-文法驱动骨架构建设计.md（🟢已实现）。

## 一、产物

**新增 3 文件**
- `config/segmentation_spec.yaml` —— 物理切分规范正本（列模型 / 词法 / paragraph 模型）。
- `config/skeleton_spec.yaml` —— 骨架文法规范正本（block_grammar / control_flow），项目核心资产。
- `config/grammar_loader.py` —— COBOL 文法访问层，统管两正本，与 spec_loader 并列。

**改动 4 文件**
- `translator/segmenter.py` —— 词法集取自 grammar_loader；split_paragraphs 调 paragraph_label（修 Bug1）。
- `parser/cobol_columns.py` —— clean_line 读 column_model.clean_slice（去 6/72 魔数）。
- `config/wsaa_translation_spec.yaml` —— 新增 `procedure_semantics.assignment_target` 规范条。
- `translator/rules.py` —— 新增 `_lvalue`；全部赋值目标点（_assign + 算术/COMPUTE）改用之（修 Bug2）；
  build_section 读 back_edge_state_machine()。

## 二、命令

```bash
# 单元测试
python -m unittest test_translation            # 24 passed, 2 skipped(无 vLLM)

# 全量确定性翻译（主类骨架）
python scripts/translate_skeleton.py \
  --in /home/zp/Documents/cob/ZPOLDWNM.cob --out /tmp/Zpoldwnm_step08.java   # 7557 行

# WSAA 零回归校验
python scripts/translate_wsaa.py --program ZPOLDWNM \
  --in "/home/zp/Documents/cob/ZPOLDWNM/拆解/ZPOLDWNMWSAA.cob" --out /tmp/wsaa_step08.java
diff "/home/zp/Documents/cob/ZPOLDWNM/拆解/ZpoldwnmWsaa.java" /tmp/wsaa_step08.java   # 空
```

## 三、校验结果

| 项 | 目标 | 实测 |
|---|---|---|
| 字面量左值（行首 / += -=） | 0 | 0（原 417） |
| `// paragraph` 注释 | 出现 | 231（原 0，Bug1 修复） |
| 纯标号误判 TODO | 0 | 0（残留 126 含段名者经核为复合语句 TODO） |
| 状态机降级段 | ~27 | 27 |
| TODO 叶子总数 | ~358 | 374（余真实复合语句盲区，出范围） |
| 花括号配平 | 平 | 1184 = 1184 |
| test_translation | 全绿 | 24 通过 / 2 skip |
| WSAA 容器类 diff | 空 | 空（零回归） |

## 四、Token 使用分析

- **主要消耗**：① 设计阶段两轮重写（窄修 bug → 两阶段文法驱动 → config 规范为核心），
  反复编辑设计文档约 6 轮；② 精读核心代码定位（segmenter/rules/cobol_columns/spec_loader），
  其中 rules.py(1247 行) 多段精读最重；③ 落地后两轮 grep 指标核对 + 算术/COMPUTE 目标点的二次修复迭代。
- **量级**：中高。本会话经历「设计-认可-细化-实现-校验」完整闭环 + 一次范围外延（COMPUTE 目标点），
  对话轮数偏多，rules.py 的多次精读是单点最大来源。
- **Context 预警**：本会话 context **已偏高**（叠加了设计多轮 + 多文件精读 + 翻译产物指标）。
  ⚠️ 建议下个任务前 `/clear` 或新开会话，避免大文件与历史对话被后续每轮复利重算。
