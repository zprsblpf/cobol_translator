# 操作记录 · 步骤二：MongoDB 入库实现 + 输出目录调整 + 包化拆分

> 执行日期：2026-05-31
> 对应设计文档：`docs/详细设计/步骤02-MongoDB入库实现与输出目录调整设计.md`（✅已认可）
> 执行人：Claude
> 结果：✅ 代码自检全部通过（入库未实跑，待环境就绪）

---

## 1. 目标

1. 补缺失的 `db/mongo_client.py`（`MongoStore`），使入库可用。
2. 调整拆解输出布局：切片入 `<out>/拆解/`，manifest 留 `<out>/` 根。
3. 按全局约定 §17 把 494 行的 `scripts/decompose.py` 拆成 `decompose/` 功能包 + 瘦入口。

## 2. 已确认的决定（用户）

- 架构索引 `项目总览.md` **覆盖整个项目**（拆解线 + 翻译线）。
- 旧产物 `/home/zp/Documents/cob/拆解/ZPOLDWNM/`：**保留不删**。
- 本步入库：**只交付代码可用**，不实跑（未提供连接串、pymongo 未装）。

## 3. 产物文件

| 文件 | 动作 | 逻辑行（实测） |
|---|---|---|
| `db/__init__.py` / `db/mongo_client.py` | 新建 | 2 / 28 |
| `decompose/__init__.py` | 新建（包标识 + 重导出） | 3 |
| `decompose/models.py` | 新建（迁入 Block） | 19 |
| `decompose/lines.py` | 新建（迁入行级处理） | 32 |
| `decompose/structure.py` | 新建（迁入结构定位） | 67 |
| `decompose/blocks.py` | 新建（迁入构块） | 79 |
| `decompose/manifest.py` | 新建（迁入写文件 + 目录调整） | 84 |
| `decompose/importer.py` | 新建（迁入 import_mongo） | 27 |
| `decompose/core.py` | 新建（迁入 decompose()） | 27 |
| `scripts/decompose.py` | 改写为瘦入口（494→约70行，逻辑38） | 38 |
| `scripts/verify_decompose.py` | 切片读取加 `拆解/` 子目录 + docstring | 61 |
| `docs/架构索引/项目总览.md` | 新建（全项目概要层） | — |

所有 .py 逻辑行（不含 docstring/注释/空行）均 ≤84，符合 §17「≈100 行」。

## 4. 执行命令

```bash
# 重新拆解到新布局
python3 scripts/decompose.py --cob /home/zp/Documents/cob/ZPOLDWNM.cob \
  --program ZPOLDWNM --out /home/zp/Documents/cob/ZPOLDWNM
# 校验
python3 scripts/verify_decompose.py --out /home/zp/Documents/cob/ZPOLDWNM
```

新产物：`/home/zp/Documents/cob/ZPOLDWNM/`（`拆解/` 134 切片 + manifest.json + manifest.md）。

## 5. 自检结果（全部 ✅）

| 检查项 | 结果 |
|---|---|
| 拆分等价性：新 manifest vs 旧 manifest 逐字节（除 generated_at） | ✅ 一致 |
| `--help` 正常输出 | ✅ |
| 新布局产物 | ✅ `拆解/*.cob` 134 + 根下 manifest.json/md |
| verify 9 项 | ✅ 全过（135 块 = 1 META + 1 WS + 133 SECTION） |
| 各文件逻辑行 ≤100 | ✅ 最大 84（manifest.py） |
| 无循环导入 | ✅ |

## 6. 后续

- Mongo 就绪后实跑入库：
  ```bash
  python3 scripts/decompose.py --import-only \
    --out /home/zp/Documents/cob/ZPOLDWNM \
    --mongo-uri "mongodb://<用户提供>/" --db cobol
  ```
  预期：首跑 inserted=135，再跑 updated=135/inserted=0（幂等），分析字段不被覆盖。
- 旧目录 `/home/zp/Documents/cob/拆解/ZPOLDWNM/` 暂保留，确认新布局无误后可由用户决定清理。
