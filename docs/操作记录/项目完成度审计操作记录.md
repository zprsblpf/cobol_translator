# 项目完成度审计

> 生成日期：2026-07-06
> 对应：Batch 6.3 — Project Completion Audit

## 项目范围

COBOL → Java 确定性翻译器。输入单个大型 COBOL 程序（ZPOLDWNM.cob），经解析 → ASG/visitor → 骨架装配路径，产出多文件 Java 主类骨架 + 方法体 + 审查清单。

## 已完成工作

### 三相架构（预处理 → ASG → visitor）

| 相 | 模块 | 状态 | 说明 |
|----|------|------|------|
| 相0 预处理 | `preprocess/` | ✅ 完成 | 列净化、方言归一、停用行跳过 |
| 相1 解析 | `parser/` | ✅ 完成 | 固定列解析、WS 变量、LINKAGE、COPY 引用 |
| 相2 ASG | `asg/` | ✅ 完成 | 类型节点树（MOVE/IF/EVALUATE/PERFORM/CALL/GOTO）、引用解析、结构改写 |
| 相3 visitor | `asg/visitor.py` | ✅ 完成 | LeafJavaVisitor 渲染、子类型 visit 分派 |

### 叶子动词翻译（translator/leaf/）

| 动词族 | 模块 | 覆盖 | 说明 |
|--------|------|------|------|
| MOVE | `move.py` | ✅ 全 | ASSIGN / RESET / FUNC_SET / 88 / FIGURATIVE |
| IF | `cond.py` | ✅ 全 | 比较、AND/OR、NOT、SPACES → isBlank |
| PERFORM 循环 | `loop.py` | ✅ 全 | UNTIL/VARYING/TIMES/TEST AFTER |
| CALL | `call.py` | ✅ 全 | IO 调用 findByKey、系统子程序、未映射→LLM |
| 算术赋值 | `arith.py`, `assign.py` | ✅ 全 | ADD/SUB/MULT/DIV/COMPUTE 及其 GIVING |
| 控制流 | `control.py` | ✅ 全 | EXIT/GOBACK/STOP/CONTINUE、GO TO |
| EVALUATE | `control.py` | ✅ 全 | switch 壳、case 标签 |
| STRING | `string.py` | ✅ 全 | DELIMITED BY SIZE/SPACE/literal |
| UNSTRING | `unstring.py` | ✅ 全 | DELIMITED BY、多目标 |
| INSPECT | `inspect.py` | ✅ 全 | TALLYING/REPLACING |
| SEARCH | `search.py` | ✅ 部分 | SEARCH ALL WHEN（其他形态→fallback） |

### 骨架装配（translator/skel/）

| 结构 | 模块 | 状态 | 说明 |
|------|------|------|------|
| PERFORM out-of-line | `perform_call.py` | ✅ 完成 | 单段/THRU 区间/合成区间方法 |
| GOTO dispatch | `flow_dispatch.py` | ✅ 完成 | 状态机/向前/回边 |
| BEGN for-each | `io_rewrite.py` | ✅ 完成 | BEGN+NEXTR → List + for |
| BEGN single | `io_rewrite.py` | ✅ 完成 | 单次定位 + isEmpty |
| READR single | `io_rewrite.py` | ✅ 完成 | findByKey + null/try-catch |
| WRITE/UPDAT/DELET | `io_rewrite.py` | ✅ 完成 | save/update/delete |

### 跨段分析（analyzer/）

| 分析 | 模块 | 状态 |
|------|------|------|
| 调用链图 | `callgraph.py` | ✅ 完成 |
| 数据流分析 | `dataflow.py` | ✅ 完成 |

### 逻辑文档生成（logician/）

| 功能 | 状态 | 说明 |
|------|------|------|
| COBOL → LogicDoc | ✅ 完成 | 词元提取、结果识别、路径追踪、交叉点合并 |
| JSON 输出 | ✅ 完成 | 稳定 key 排序 |
| Markdown 输出 | ✅ 完成 | 入口→结果树→路径流程图→词元表 |
| check.py 集成 | ✅ 完成 | `--suite logic` |
| Java 侧分析 | 📅 待规划 | 解析 ZpoldwnmServiceImpl.java |

### 验证基础设施

| 闸口 | 套件 | 测试数 |
|------|------|--------|
| quick | smoke + parse-only | ~6 |
| leaf | 叶子动词单测 | ~80 |
| asg | ASG/visitor + diff | ~56 |
| logic | logician | 45 |
| all | 全量集成 | 263 (skipped=2) |

## 验证命令

```powershell
# 快速冒烟
python scripts/check.py --suite quick

# 叶子翻译
python scripts/check.py --suite leaf

# ASG/visitor
python scripts/check.py --suite asg

# 逻辑文档
python scripts/check.py --suite logic

# 全量（最终闸口）
python scripts/check.py --suite all

# 大样本冒烟
python scripts/translate_skeleton.py --in <large>.cob --out /tmp/out.java --limit 10 --stats

# 逻辑文档生成
python scripts/generate_logic_doc.py --cob tests/fixtures/minimal.cob --out-dir output/logic-doc

# ASG vs legacy 逐字符比对
python scripts/diff_asg_vs_legacy.py tests/fixtures/minimal.cob --verb SECTION
```

## 剩余 fallback 清单

| 项 | 位置 | 性质 | 优先级 |
|----|------|------|--------|
| SEARCH 非 ALL 形态 | `translator/leaf/search.py` | 未实现 → fallback | 低（语料中少用） |
| STRING 指针/溢出 | `translator/leaf/string.py` | 未实现 → TODO | 低（语料中少用） |
| SET 语句 | `translator/leaf/assign.py` | 部分（SET TO true → LLM） | 低 |
| 入口代码段 | parser `__ENTRY` | 已实现但未验证全量 | 低 |
| Java 侧逻辑文档 | `logician/` | 待设计 | 中（用户指定方向） |
| 88 条件名展开 | `logician/tokens.py` | stub，待接 WS 树 | 低 |

## 未覆盖的大批量任务

- 对 ZPOLDWNM.cob（~133 SECTION）的全量翻译验证（需外部夹具）
- Java 编译校验 `validate_with_javac`（需 JDK 17/21 环境）
- 全量 path LLM 翻译（需 vLLM/OpenAI 兼容服务）

## 项目健康度

| 指标 | 值 |
|------|-----|
| 测试总数 | 263 (skipped=2) |
| 测试通过率 | 100% |
| 逻辑行（logician 以外） | ~12000 |
| 逻辑行（logician） | ~1100 |
| 0-day 风险 | 低（无外部服务依赖的核心翻译路径已全覆盖） |
