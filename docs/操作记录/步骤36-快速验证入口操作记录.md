# 步骤36 快速验证入口操作记录

日期：2026-07-01

## 本次改动

- `scripts/check.py`
  - 增加 `--suite quick|leaf|asg|deterministic|all`。
  - 默认 suite 为 `all`，保持 `python scripts/check.py` 等价完整本地闸口。
  - 拆出 suite 命令配置函数，便于不启动子进程的配置测试覆盖。
- `test_smoke.py`
  - 增加 `TestCheckSuiteConfig`，验证各 suite 的命令配置与默认行为，不执行 subprocess。
- `docs/运行验收.md`
  - 增加 focused suite 使用说明。

## 验收

- `python -m unittest test_smoke.TestCheckSuiteConfig -v`
  - 6 tests OK。
- `python scripts/check.py --suite quick`
  - exit 0；8 tests OK，并完成 minimal parse-only。
- `python scripts/check.py --suite leaf`
  - exit 0；62 tests OK。
- `python scripts/check.py --suite asg`
  - exit 0；69 tests OK。
- `python scripts/check.py --suite deterministic`
  - exit 0；完成 minimal skeleton 与 `main.py --no-llm` 输出。
- `python scripts/check.py --suite all`
  - exit 0；221 tests OK，skipped=2，并完成 minimal parse-only、skeleton、no-LLM 翻译检查。

## unsupported rule_id

- 本次未新增 unsupported rule_id。

## 遗留风险

- focused suite 依赖 unittest `-k` 的 shell-style pattern 行为，当前 Python 3.12 验收通过。
