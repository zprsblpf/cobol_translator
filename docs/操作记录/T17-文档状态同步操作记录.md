# T17 文档状态同步操作记录

## 目标

同步当前实现状态到文档索引与验收说明，只更新 `docs/`，不修改生产代码。

## 本次修改

- `docs/架构索引/项目总览.md`
  - 补齐步骤31-36 的详细设计索引。
  - 在 leaf 公用底座表中补充 `translate_leaf_stmt` 统一入口、`unstring.py`、`inspect.py`、`search.py`，并更新 `assign.py` 的步骤36组字段初始化能力。
  - 更新 `scripts/diff_asg_vs_legacy.py` 说明为当前 `VERB_MATRIX`：`MOVE|IF|PERFORM|PERFORMCALL|FLOW|IO|CALL|ARITH|FALLBACK|CONTROL|SECTION`。
  - 在 `translator.skeleton_gen` 简述中补充 T15：ASG 渲染异常回退 legacy 时记录 `Ctx.asg_fallback_events`，并纳入 `check.py --suite asg`。
- `docs/运行验收.md`
  - 增加 `python scripts/check.py --suite quick|leaf|asg|all` 的分组验收说明。
  - 补充 T15 fallback observability 的验收归属。
  - 增加 ASG vs legacy diff 闸口与当前矩阵动词列表。

## 已确认状态

- 步骤31-36 已有对应详细设计与操作记录。
- T15 操作记录已存在：`docs/操作记录/T15-ASG-fallback-observability.md`。
- diff 工具矩阵操作记录已存在：`docs/操作记录/diff工具动词矩阵操作记录.md`。

## 验证命令

```powershell
rg -n "步骤31|步骤32|步骤33|步骤34|步骤35|步骤36|T15|VERB_MATRIX|quick\\|leaf\\|asg\\|all" docs -S
```
